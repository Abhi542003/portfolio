# Tasks - Contact Form Secure Email Integration

- [x] Install fullstack dependencies: `express`, `cors`, `dotenv`, `nodemailer`, `resend`, and `concurrently`
- [x] Update `package.json` scripts to run both Vite and the server concurrently
- [x] Create `server.js` at the root with validation, admin notifications, and visitor responsive HTML emails
- [x] Configure server proxying in `vite.config.js` for clean `/api` fetching
- [x] Implement local environment templates in `.env.example`
- [x] Upgrade the React `Contact.jsx` component to fetch the server, handle spinner locks, and show success toast notifications
- [x] Verify production build compiles cleanly and run Vite dev server
